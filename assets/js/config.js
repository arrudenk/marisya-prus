/**
 * config.js — НАЛАШТУВАННЯ. Єдине місце, яке правиш при підключенні таблиці.
 *
 * Поки SHEET_ID порожній — сайт працює на вбудованому контенті (content.js),
 * таблиця вимкнена. Щоб увімкнути редагування через Google-таблицю, встав
 * сюди ID таблиці (див. SHEET.md, крок за кроком).
 */

/** ID Google-таблиці. Береться з її URL між "/d/" і "/edit":
 *  docs.google.com/spreadsheets/d/[ОЦЕЙ_ID]/edit
 *  Порожній рядок = таблиця вимкнена, сайт на вбудованому контенті. */
export const SHEET_ID = '153IgzYqFPVk6kzmO78rFmnEzs0-O26UvoDnq1qcyVE8';

/** GID аркуша (вкладки). Перший аркуш завжди має gid=0. Якщо береш інший
 *  аркуш — його gid видно в URL таблиці після "gid=". */
export const SHEET_GID = '0';

/** Ширина картинок із Google Drive у пікселях.
 *  grid — для сітки на сторінці; full — для великого перегляду в лайтбоксі.
 *  Більше число = краща якість і більша вага. */
export const IMG = { grid: 1600, full: 2400 };
